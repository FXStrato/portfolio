//Imports necessary components for displaying information to client. Big one is the Router, which
//imports the router component which in turn routes to all other pages

import angular from 'angular';
import angularMeteor from 'angular-meteor';

import { name as Router } from '../imports/ui/components/router/router';
